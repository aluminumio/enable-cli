name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: enbl-linux-amd64
          - os: macos-15
            artifact: enbl-darwin-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: aluminumio/install-crystal@master
      - run: mkdir -p bin && crystal build src/enable.cr -o bin/${{ matrix.artifact }} --release --no-debug
      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: bin/${{ matrix.artifact }}

  publish:
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Read version
        id: version
        run: |
          version=$(grep '^version:' shard.yml | awk '{print $2}')
          echo "version=$version" >> $GITHUB_OUTPUT
          if gh release view "v$version" &>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Create release
        if: steps.version.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "v${{ steps.version.outputs.version }}" artifacts/* --generate-notes

      - name: Notify Homebrew tap
        if: steps.version.outputs.exists == 'false'
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.REPO_DISPATCH_TOKEN }}
          repository: aluminumio/homebrew-tap
          event-type: enable-cli-released
          client-payload: '{"version": "${{ steps.version.outputs.version }}"}'
